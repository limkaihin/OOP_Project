package com.example.app;

/**
 * Entry point class kept for launcher compatibility.
 * All lifecycle + rendering code lives in {@link GameMaster}.
 */
public class Main extends GameMaster {
    // Intentionally empty.
}

package com.example.app.engine.io;

/**
 * Generic actions (not context-specific).
 * Bind these to keys/buttons in InputBindings.
 */
public enum InputAction {
    CONFIRM,
    BACK,
    PAUSE,

    MOVE_LEFT,
    MOVE_RIGHT,
    MOVE_UP,
    MOVE_DOWN,

    // NEW:
    SPAWN,

    ACTION_1,
    ACTION_2
}
